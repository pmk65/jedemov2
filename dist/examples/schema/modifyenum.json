{
  "title": "Output",
  "type": "object",
  "theme": "bootstrap4",
  "required": ["test1","test"],
  "properties": {
    "test1": {
      "title": "String test field",
      "type": "string",
      "default": "Hello World"
    },
    "test": {
      "title": "String enum field",
      "type": "string",
      "enum": ["black","white"]
    }
  }
}